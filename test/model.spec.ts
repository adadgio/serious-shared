import * as assert from 'assert'
import * as chai from 'chai'
const expect = chai.expect

describe('Models test', () => {

    it('Profile prropertiies are set and merged successfully', () => {


        // https://medium.com/building-ibotta/testing-arrays-and-objects-with-chai-js-4b372310fe6d
        // expect(fields).to.eql([ 'cuid', 'wuid', 'name', 'type', 'private', 'hash', 'members', 'general', 'updated' ])
        // expect(values).to.eql([ channel.cuid, workspace.wuid, 'General', 'chan', 0, null, 0, 0, 'x' ])
    })
})
